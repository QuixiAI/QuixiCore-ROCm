*.i
*.ii
*.gpu
*.ptx
*.cubin
*.fatbin
*.txt
*.d
*.ncu-rep
__pycache__/
*/printouts/*
tests/unit/build/
tests/unit/*/build/
tests/unit/*/unit_tests
hsakmt_counters.csv
*.o
build/
kernels/**/tk_kernel
.build-logs/
*.egg-info/
*.pyc
*.pyo
*.pyd
*.pyw
*.pyz
*.pywz
*.rocprofv3
*.so
*.amdgcn
utils/__pycache__/
*x86_64-unknown-linux-gnu*
*amdhsa.hipfb
*amd-amdhsa-gfx950.hipi
*amd-amdhsa-gfx950.bc

# Host-specific build/test scripts.
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/*.sh
# ...but the autoresearch protocol scripts are NOT host-specific: they are the
# measure-and-verify protocol itself and must survive a fresh checkout, because
# the loop that uses them runs across many agent restarts.
!kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/hk_*.sh
# gate.sh is the same kind of thing and misses the hk_ prefix only by age. It is the
# byte-exact correctness gate for the ladder and is cited by name from README.md,
# GL_LAYOUT_REFACTOR.md and TN_MIGRATION.md, so it is un-ignored rather than renamed.
!kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/gate.sh
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/*.sbatch
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/*.out
# The rules above are anchored to one directory, so retiring a host-specific
# script into archive/ would otherwise un-ignore it.
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/archive/*.sh
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/archive/*.sbatch
kernels/matmul/bf16fp32/variants/rocm_udna1/gfx1250/archive/*.out

# Local reference source mirrors.
.reference/

# Local environments and editor files.
.venv/
.env
.env.*
.DS_Store
.vscode/
.idea/
*.swp
*~

!CMakeLists.txt

# Standalone kernel-harness binaries (kernels/**/variants/**, kernels/common)
*.out
kernels/activations/situ/variants/rocm_cdna3/*.out
