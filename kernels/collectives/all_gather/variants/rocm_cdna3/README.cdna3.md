# All Gather — CDNA3 (RCCL)

Standalone ROCm/RCCL coverage for CUDA `parallel/all_gather`.

Run with one process per GPU:

```bash
make test
```

Override `GPUS`, `NPROC`, and `N` for full-node or larger payload runs.
