# All To All — CDNA3 (RCCL)

Standalone ROCm/RCCL coverage for CUDA `parallel/all_to_all`.

Run with one process per GPU:

```bash
make test
```

Override `GPUS`, `NPROC`, and `N` for full-node or larger payload runs.
