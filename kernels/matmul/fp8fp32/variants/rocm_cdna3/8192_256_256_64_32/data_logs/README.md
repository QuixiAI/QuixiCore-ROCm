# Upstream run logs

`0613_003858_outputs/` is an upstream HipKittens run archive, imported verbatim
with the 2026-08-25 upstream sync (upstream path
`kernels/cdna3/gemm/fp8fp32/mi300x/8192_256_256_64_32/data_logs/`). It is a
snapshot of the kernel source, its Makefile, its test, and the JSON that run
emitted — an upstream measurement, not a QuixiCore-ROCm one, and not reproduced
on this backend. QuixiCore measurements live in `perf/results/` with the
provenance that `perf/perf.md` requires; use those for any claim.
