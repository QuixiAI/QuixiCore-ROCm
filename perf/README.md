# Performance

The target QuixiCore performance layout is reserved here. Current ROCm benchmark
and profiling workflows live under `analysis/`, `docs/profiling/`, and
kernel-local scripts.

Use `scripts/bench` as the common entrypoint once benchmark harnesses move under
`perf/`.
